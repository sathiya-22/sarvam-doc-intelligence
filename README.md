# 📄 Sarvam Document Intelligence
Extract text from Indian language PDFs/images → detect language → translate to English.

```bash
pip install -r requirements.txt && cp .env.example .env
python main.py document.pdf output.json
python main.py scan.png output.json
```
